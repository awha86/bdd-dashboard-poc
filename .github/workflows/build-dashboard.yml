name: Build Allure Dashboard

on:
  push:
    branches: [master]     # Skift til [main] hvis I bruger main
    paths:
      - 'data/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1) Hent kun din kode fra master/main
      - name: Checkout code (master)
        uses: actions/checkout@v4
        with:
          ref: master         # eller main
          fetch-depth: 1
          submodules: false

      # 2) Manuel fetch af gh-pages til pages-mappen (ingen checkout@v4 her)
      - name: Fetch gh-pages history
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir pages
          cd pages
          git init
          git remote add origin https://x-access-token:${GH_TOKEN}@github.com/${{ github.repository }}.git
          git fetch --depth=1 origin gh-pages
          git checkout --detach FETCH_HEAD

      # 3) Installér Java
      - name: Install Java
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-11-jre-headless

      # 4) Installér Allure CLI
      - name: Install Allure CLI
        run: |
          curl -o allure.tgz -L https://github.com/allure-framework/allure2/releases/download/2.34.0/allure-2.34.0.tgz
          tar -xzf allure.tgz
          sudo mv allure-2.34.0 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/local/bin/allure

      # 5) Debug: vis test-resultater
      - name: List result folders
        run: |
          echo "=== data/integration ==="
          ls -lR data/integration

      # 6) Kopiér tidligere history ind i resultater
      - name: Copy previous history into results
        run: |
          mkdir -p data/integration/history
          cp -r pages/allure-report/history/* data/integration/history/

      # 7) Generér ny rapport inkl. history
      - name: Generate Allure report
        run: |
          echo "=== data/integration med history ==="
          ls -lR data/integration
          allure generate data/integration/* --clean -o allure-report

      # 8) Flyt rapport til root (til Pages)
      - name: Copy report to root
        run: |
          cp -r allure-report/. .

      # 9) Deploy til gh-pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: .
          publish_branch: gh-pages
          exclude_assets: ''
